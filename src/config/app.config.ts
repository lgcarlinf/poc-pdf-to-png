export const APP_CONFIG = {
  PORT: 3000,
  PATHS: {
    UPLOADS: "uploads",
    OUTPUT: "output",
  },
  CONVERSION: {
    OUTPUT_MASK: "page",
    VIEWPORT_SCALE: 2.0,
    PASSWORD: "",
  },
} as const;
